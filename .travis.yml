sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t justjudy/docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true justjudy/docker-react-app npm run test -- --corverage

after_success:
  - echo "Test Success"